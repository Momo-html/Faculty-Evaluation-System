<!DOCTYPE html>
<html>
<head>
    <style>
        body { font-family: 'Helvetica', sans-serif; color: #333; }
        .header { text-align: center; border-bottom: 2px solid #006835; padding-bottom: 10px; }
        .title { font-size: 22px; font-weight: bold; color: #006835; }
        .info-section { margin-top: 30px; }
        .label { font-weight: bold; color: #8B7232; }
        .stats-table { width: 100%; margin-top: 20px; border-collapse: collapse; }
        .stats-table th, .stats-table td { padding: 12px; border: 1px solid #ddd; text-align: left; }
        .score-box { background: #f4f4f4; padding: 20px; text-align: center; margin-top: 20px; border-radius: 10px; }
        .footer { position: fixed; bottom: 0; width: 100%; text-align: center; font-size: 10px; color: #777; }
    </style>
</head>
<body>
    <div class="header">
        <div class="title">FACULTY EVALUATION SYSTEM</div>
        <div>Academic Year {{ $school_year }} | {{ $semester }}</div>
    </div>

    <div class="info-section">
        <p><span class="label">Faculty Name:</span> {{ $faculty->name }}</p>
        <p><span class="label">Date Generated:</span> {{ $date }}</p>
    </div>

    <table class="stats-table">
        <thead>
            <tr style="background-color: #006835; color: white;">
                <th>Metric</th>
                <th>Result</th>
            </tr>
        </thead>
        <tbody>
            <tr>
                <td>Total Expected Evaluations</td>
                <td>{{ $totalExpected }}</td>
            </tr>
            <tr>
                <td>Responses Received</td>
                <td>{{ $totalReceived }}</td>
            </tr>
            <tr>
                <td>Participation Rate</td>
                <td>{{ number_format($rate, 1) }}%</td>
            </tr>
        </tbody>
    </table>

    <div class="score-box">
        <div style="font-size: 14px;">Overall Participation Status</div>
        <div style="font-size: 32px; font-weight: bold; color: {{ $rate >= 80 ? '#2e7d32' : '#e65100' }};">
            {{ $rate >= 80 ? 'READY' : 'INCOMPLETE' }}
        </div>
    </div>

    <div class="footer">
        Generated by FEU Evaluation Analytics - Confidential Report
    </div>
</body>
</html>